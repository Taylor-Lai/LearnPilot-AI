# 后端 API 主机地址（仅主机，不含 /api 路径后缀）
# 开发环境留空，通过 Vite 代理转发 /api、/resources 等路径
VITE_API_BASE_URL=

# 本地开发时 Vite 代理目标（可选，默认 http://127.0.0.1:8001）
# VITE_DEV_API_TARGET=http://127.0.0.1:8001

# 生产环境示例：
# VITE_API_BASE_URL=https://api.example.com
